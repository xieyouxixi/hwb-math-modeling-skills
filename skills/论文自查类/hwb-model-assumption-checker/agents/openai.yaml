interface:
  display_name: "HWB 模型假设自查"
  short_description: "诊断假设的必要性、合理性、数据相容性与被使用情况"
  brand_color: "#B4232A"
  default_prompt: "使用 $hwb-model-assumption-checker 检查我的模型假设：逐条判断是否必要、合理、与数据一致，并核对每条假设是否在后续模型章节中被真正使用。"
policy:
  allow_implicit_invocation: true
